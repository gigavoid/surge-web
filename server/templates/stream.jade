html
    head
        title='Surge - ' + stream.name
        link(rel='stylesheet', href='/static/app.css')
        link(rel='stylesheet', href='http://fonts.googleapis.com/css?family=Open+Sans')
    body
        .wrapper
            #player.stream(streamer=stream.name)
            a(href='/' style='float: right;')
                h2 SURGE
            h2=stream.name
            p='Views: ' + stream.viewers
        script(src='http://jwpsrv.com/library/SV1SatTmEeSTUgp+lcGdIw.js')
        script(src='/static/app.js')
